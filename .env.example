# Copier ce fichier en .env.local et renseigner les valeurs — NE JAMAIS COMMITER .env.local

# ── Assistant IA (Option A — Claude Code uniquement) ───────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── Base de données Azure dev (fournie par Benjamin) ──────────────────────
DATABASE_URL=mysql+pymysql://<user>:<password>@<host>.mysql.database.azure.com/bizplan?ssl_ca=DigiCertGlobalRootG2.crt.pem

# ── JIRA MCP ───────────────────────────────────────────────────────────────
JIRA_EMAIL=benjamin.guerin@ionis-stm.com
JIRA_API_TOKEN=<token_atlassian>

# ── Azure (lecture seule en dev, injection par CI/CD en prod) ─────────────
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_SUBSCRIPTION_ID=
